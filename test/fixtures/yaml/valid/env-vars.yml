monitors:
  - name: Prod Monitor
    type: HTTP
    config:
      url: ${APP_URL:-https://default.example.com}
      method: GET

alertChannels:
  - name: slack-channel
    config:
      channelType: slack
      webhookUrl: ${SLACK_WEBHOOK:-https://hooks.slack.com/default}
